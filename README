npm install express mysql2 dotenv
npm install --save-dev nodemon

__________________________________
Db
CREATE DATABASE motosmart;

USE motosmart;

CREATE TABLE motos (
  id INT AUTO_INCREMENT PRIMARY KEY,
  marca VARCHAR(100),
  modelo VARCHAR(100),
  cilindraje INT
);